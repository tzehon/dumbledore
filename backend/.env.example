# MongoDB Configuration
# For local MongoDB installation:
# MONGO_URI="mongodb://localhost:27017"

# For MongoDB Atlas (cloud) - basic username/password authentication:
# Note: This is a basic way of authentication. In a production environment, 
# you might use other available forms of authenticating to the database such as:
# - X.509 certificates
# - AWS IAM roles
# - LDAP authentication
# - SCRAM authentication with stronger credentials
# MONGO_URI="mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority&appName=YourAppName"

# Replace with your actual MongoDB connection string
MONGO_URI="mongodb://localhost:27017"

# Database name
DB_NAME="comms_db"